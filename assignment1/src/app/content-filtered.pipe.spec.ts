import { ContentFilteredPipe } from './content-filtered.pipe';

describe('ContentFilteredPipe', () => {
  it('create an instance', () => {
    const pipe = new ContentFilteredPipe();
    expect(pipe).toBeTruthy();
  });
});
